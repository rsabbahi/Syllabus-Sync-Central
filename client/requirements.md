## Packages
(none needed)

## Notes
The application relies on the base stack and shadcn/ui components already installed.
Authentication is handled by Replit Auth (useAuth hook provided).
File uploads expect standard multipart/form-data.
